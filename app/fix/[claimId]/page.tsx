"use client";import{useState}from'react';import{claims,codes,users}from'../../../lib/data';export default function Fix({params}:{params:{claimId:string}}){const c=claims[params.claimId];const code=codes[c.rejectionCode as keyof typeof codes];const[done,setDone]=useState(false);const[msg,setMsg]=useState(false);const[resub,setResub]=useState(false);if(!c)return null;const timeline=[...c.history,...(c.id==='CLM-1003'&&resub?[{date:'Current',event:'Resubmitted — under review'}]:[{date:'Current',event:c.id==='CLM-1001'?'Waiting on employer':'Action needed'}])];return <main className="shell"><p className="demo">Demo data — not connected to EPFO</p><h1>{c.id==='CLM-1001'?'Waiting on Vantage Retail Pvt Ltd':'Fix your bank details'}</h1>{code.fixSteps.map((x,i)=><label className="check" key={x}><input type="checkbox" onChange={()=>setDone(i>=0)}/><span>{x}</span></label>)}{c.actor==='self'?<><label>Correct bank account number<input placeholder="Enter account number"/></label><label>Correct IFSC<input placeholder="Enter IFSC"/></label><button onClick={()=>setResub(true)} disabled={!done}>{resub?'Resubmitted':'Resubmit claim'}</button>{resub&&<p className="card">Expected turnaround: {('turnaround'in code&&code.turnaround)||''}</p>}</>:<><button onClick={()=>setMsg(true)}>Draft a message to your employer</button>{msg&&<section className="card"><p className="demo">Demo data — not connected to EPFO</p><textarea readOnly value={`Hi HR team, I've filed my PF withdrawal (UAN ${c.uan}, claim ${c.id}) and it was rejected because my KYC (Aadhaar/PAN/Bank) is still pending your approval on the EPFO employer portal, under 'Member KYC Approval'. Could you approve it at your end this week? Once approved I don't need to refile — it resumes automatically. Thanks!`}/><button onClick={()=>navigator.clipboard.writeText((document.querySelector('textarea') as HTMLTextAreaElement).value)}>Copy</button></section>}</>}<h2>Status tracker</h2><section className="timeline">{timeline.map(x=><div className="event" key={x.date+x.event}><strong>{x.event}</strong><br/><span className="muted">{x.date}</span></div>)}</section></main>}